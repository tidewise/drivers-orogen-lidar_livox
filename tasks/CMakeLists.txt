# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(lidar_livoxTaskLib)

ADD_LIBRARY(${LIDAR_LIVOX_TASKLIB_NAME} SHARED
    ${LIDAR_LIVOX_TASKLIB_SOURCES})
add_dependencies(${LIDAR_LIVOX_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${LIDAR_LIVOX_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${QT_LIBRARIES}
    ${LIDAR_LIVOX_TASKLIB_DEPENDENT_LIBRARIES}
    livox_lidar_sdk_shared)

SET_TARGET_PROPERTIES(${LIDAR_LIVOX_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${LIDAR_LIVOX_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${LIDAR_LIVOX_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${LIDAR_LIVOX_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${LIDAR_LIVOX_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${LIDAR_LIVOX_TASKLIB_HEADERS}
    DESTINATION include/orocos/lidar_livox)

